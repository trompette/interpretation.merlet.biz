#!/usr/bin/env bash

cd "$(dirname $0)/.." || exit
git fetch --all --prune
git reset origin/master --hard
php bin/composer self-update --clean-backups
php bin/composer install --no-dev --optimize-autoloader --verbose
echo Deployed.
